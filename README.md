# KPO
# Finance Tracker - HSE Bank

Модуль учета личных финансов для приложения ВШЭ-Банк.

**Требования:** .NET 8.0+

**Кодстайл:** Проект соответствует стандартам C#. Для проверки используется `.editorconfig`.

---

## Реализованная функциональность (3 балла)

### Основные требования
- ✅ **CRUD для счетов** (`AccountFacade.cs`): создание, редактирование, удаление, просмотр
- ✅ **CRUD для категорий** (`CategoryFacade.cs`): создание, редактирование, удаление, просмотр
- ✅ **CRUD для операций** (`OperationFacade.cs`): создание, редактирование, удаление, просмотр

### Опциональная функциональность
- ✅ **Аналитика** (`AnalyticsFacade.cs`):
    - Разница доходов/расходов за период
    - Группировка по категориям
    - Итоги по доходам и расходам
- ✅ **Импорт/экспорт** (`CsvImporter.cs`, `JsonImporter.cs`, `YamlImporter.cs`, `ExportVisitor.cs`):
    - Экспорт в CSV, JSON, YAML
    - Импорт из CSV, JSON, YAML
- ✅ **Пересчет баланса** (`BalanceRecalculator.cs`):
    - Автоматический пересчет одного счета
    - Ручной пересчет всех счетов
- ✅ **Измерение времени** (`TimedCommandDecorator.cs`):
    - Время выполнения команд в миллисекундах

---

## Паттерны GoF (4 балла = 8 паттернов × 0.5)

### 1. Factory (Порождающий)
**Файл:** `Domain/Factories/EntityFactory.cs`  
**Зачем:** Централизованное создание объектов с валидацией. Гарантирует, что невалидные объекты (отрицательная сумма, пустое имя) не могут быть созданы.  
**Метод:** `CreateOperation()`, `CreateAccount()`, `CreateCategory()`

### 2. Facade (Структурный)
**Файлы:** `Core/Services/Facades/AccountFacade.cs`, `CategoryFacade.cs`, `OperationFacade.cs`, `AnalyticsFacade.cs`  
**Зачем:** Упрощает сложные операции. Например, создание операции автоматически обновляет баланс счета - клиенту не нужно знать эти детали.  
**Пример:** `OperationFacade.CreateOperation()` - создает операцию И обновляет баланс счета в одном вызове

### 3. Command (Поведенческий)
**Файлы:** `Core/Services/Commands/CreateOperationCommand.cs`, `DeleteOperationCommand.cs`  
**Зачем:** Инкапсулирует действия пользователя в объекты. Позволяет легко добавить логирование, отмену операций, очередь команд.  
**Метод:** `Execute()`

### 4. Decorator (Структурный)
**Файл:** `Core/Services/Commands/TimedCommandDecorator.cs`  
**Зачем:** Добавляет измерение времени к любой команде без изменения её кода. Соблюдает Open/Closed Principle.  
**Использование:** Оборачивает любую `ICommand` и выводит время выполнения

### 5. Template Method (Поведенческий)
**Файлы:** `Core/Services/Import/DataImporter.cs`, `CsvImporter.cs`, `JsonImporter.cs`, `YamlImporter.cs`  
**Зачем:** Общий алгоритм импорта (чтение → парсинг → валидация → сохранение), но парсинг различается для CSV/JSON/YAML.  
**Метод:** `Import()` - шаблонный метод, `ParseContent()` - изменяемый шаг

### 6. Visitor (Поведенческий)
**Файл:** `Domain/Visitors/ExportVisitor.cs`  
**Зачем:** Добавляет экспорт в разные форматы без изменения классов `BankAccount`, `Category`, `Operation`.  
**Метод:** `Visit(BankAccount)`, `Visit(Category)`, `Visit(Operation)`

### 7. Proxy (Структурный)
**Файл:** `Core/Repositories/CachedRepository.cs`  
**Зачем:** Добавляет кэширование к репозиторию. Первое чтение загружает данные в кэш, последующие читают из кэша. Улучшает производительность.  
**Метод:** `GetById()`, `GetAll()` - читают из кэша, `Add()` - пишет в базу и кэш

### 8. Strategy (Поведенческий)
**Файл:** `Core/Services/BalanceRecalculator.cs`  
**Зачем:** Разные стратегии пересчета: один счет или все счета.  
**Методы:** `RecalculateAccount()` - стратегия для одного, `RecalculateAll()` - стратегия для всех

---

##  Принципы SOLID (2 балла)

### Single Responsibility Principle
Каждый класс имеет одну ответственность:
- `EntityFactory` - только создание объектов
- `AccountFacade` - только работа со счетами
- `BalanceRecalculator` - только пересчет балансов
- `ExportVisitor` - только экспорт данных

### Open/Closed Principle
Классы открыты для расширения, закрыты для изменения:
- Новые форматы импорта добавляются через наследование `DataImporter` без изменения базового класса
- Новые visitor'ы не требуют изменений в `BankAccount`, `Category`, `Operation`

### Liskov Substitution Principle
Наследники полностью заменяют базовые типы:
- `CachedRepository<T>` заменяет `InMemoryRepository<T>` без изменения поведения
- Все `DataImporter` (Csv, Json, Yaml) работают через метод `Import()`

### Interface Segregation Principle
Маленькие специфичные интерфейсы вместо больших:
- `IEntityFactory` - только создание
- `IRepository<T>` - только CRUD
- `ICommand` - только `Execute()`
- `IEntityVisitor` - только `Visit()`

### Dependency Inversion Principle
Зависимости через абстракции, а не конкретные классы:
- `OperationFacade` зависит от `IEntityFactory`, `IRepository<Operation>`, а не от конкретных реализаций
- Все зависимости внедряются через конструктор и разрешаются DI-контейнером

---

## Принципы GRASP (входят в 2 балла)

### High Cohesion (Высокая связность)
Методы класса работают с одними и теми же данными:
- `AnalyticsFacade` - все методы работают только с аналитикой
- `CategoryFacade` - все методы работают только с категориями

### Low Coupling (Слабая связанность)
Минимальная зависимость между классами:
- Зависимости через интерфейсы, а не конкретные классы
- DI-контейнер управляет созданием объектов
- Facade скрывает сложность взаимодействия между компонентами

---

## DI-контейнер (1 балл)

**Файл:** `Program.cs`  
**Библиотека:** `Microsoft.Extensions.DependencyInjection`

Все зависимости регистрируются в DI-контейнере:
- Фабрики: `IEntityFactory → EntityFactory`
- Репозитории: `IRepository<T> → CachedRepository<T>` (с Proxy-кэшированием)
- Фасады: `AccountFacade`, `CategoryFacade`, `OperationFacade`, `AnalyticsFacade`
- Сервисы: `BalanceRecalculator`, `ConsoleUI`

Автоматическое разрешение зависимостей через `GetRequiredService<T>()`.

---

## 📊 Структура проекта
```
FinanceTracker/
├── Domain/                - Доменная модель
│   ├── Models/...         - BankAccount, Category, Operation
│   ├── Factories/...      - Factory паттерн
│   └── Visitors/...       - Visitor паттерн
├── Core/                  - Бизнес-логика
│   ├── Repositories/...   - Proxy паттерн, хранилища
│   └── Services/
│       ├── Facades/...    - Facade паттерн
│       ├── Commands/...   - Command + Decorator паттерны
│       └── Import/...     - Template Method паттерн
├── UI/...                 - Консольный интерфейс
└── Program.cs             - Точка входа, DI-контейнер
```

---

## 📝 Примеры использования

### Создание операции с измерением времени
```
3. Manage Operations → 1. Create Operation
[TIMING] CreateOperation: 12ms
```

### Аналитика
```
4. Analytics
Total Income: $75,500.00
Total Expense: $6,200.00
Difference: $69,300.00
```

### Экспорт данных
```
5. Export Data → Format: csv
Exported to export_20241205_143022.csv
```

### Импорт данных
```
6. Import Data → File: operations.csv → Format: csv
Import successful!
```

### Пересчет баланса
```
7. Recalculate Balance → 2. Recalculate all accounts
All balances recalculated!
```

---

## Соответствие критериям

| Критерий | Баллы | Статус |
|----------|-------|--------|
| Основная функциональность | 3 | ✅ Все CRUD операции, аналитика, импорт/экспорт, пересчет |
| Паттерны GoF (8 шт) | 4 | ✅ Factory, Facade, Command, Decorator, Template Method, Visitor, Proxy, Strategy |
| SOLID + GRASP | 2 | ✅ Все 5 принципов SOLID, High Cohesion, Low Coupling |
| DI-контейнер | 1 | ✅ Microsoft.Extensions.DependencyInjection |
| **ИТОГО** | **10** | ✅ |

---
